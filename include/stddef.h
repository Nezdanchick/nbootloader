#pragma once

#define OSDEV

#include <stdbool.h>
#include <stdint.h>
#include <stdnull.h>

// void *malloc(size_t);          // memory allocate
// void *calloc(size_t, size_t);  // clear allocate
// void *realloc(void *, size_t); // reallocate memory
// void free(void *);             // free memory